# DeepSeek API（必填）
# 注意：Tauri 桌面形态下，未配置此变量时应用仍可启动，
#       用户可在应用内「设置」Modal 中填入 API Key（运行时注入，不持久化）。
#       Web 形态下必须配置，否则 /api/chat 调用会返回错误。
DEEPSEEK_API_KEY=sk-your-api-key-here

# 模型选择：deepseek-v4-pro | deepseek-v4-flash
# flash 更便宜、更快；pro 推理更强。Phase 1 MVP 默认 flash。
DEEPSEEK_MODEL=deepseek-v4-flash

# Lean4 可执行文件路径。若已加入 PATH 直接用 "lean"。
# Windows 示例: C:\elan\bin\lean.exe
# Linux/macOS 示例: /usr/local/bin/lean
# Tauri 桌面形态下，应用启动时会自动检测 lean --version 是否可用；
#   不可用时弹引导 Modal 指引用户安装 elan。
LEAN_BIN_PATH=lean

# 后端监听地址
# 警告：0.0.0.0 会监听所有网络接口，仅适合本地开发。
#       生产部署（尤其是公网服务器）请改为 127.0.0.1:3000，
#       或保持 0.0.0.0:3000 但用反向代理（nginx 等）转发并加上鉴权。
#       本项目后端无任何鉴权，直接暴露 0.0.0.0 到公网会被任意人调用 API。
LISTEN_ADDR=0.0.0.0:3000

# 日志级别：error|warn|info|debug|trace
RUST_LOG=info
